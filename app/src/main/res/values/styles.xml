<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="BaseTheme.Night" parent="Theme.AppCompat.NoActionBar">
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseTheme">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ViewerTheme" parent="BaseTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <!-- ActionBar Theme Start -->
    <style name="MyActionBarPopupDrak" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@color/black_backgroud</item>
        <item name="android:textColor">@color/black_text_light</item>
    </style>
    <style name="MyActionBarPopup" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">?attr/colorBackground</item>
        <item name="android:textColor">?attr/colorTextLight</item>
    </style>
    <style name="MyActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">?attr/colorPrimary</item>
        <item name="android:actionModeBackground">?attr/colorPrimary</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:windowActionModeOverlay">true</item>
    </style>
    <!-- ActionBar Theme Start -->
    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">?attr/colorPrimaryLight</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabTextAppearance">@style/MyCustomTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/textColorSecondaryInverseSelecte</item>
        <!--<item name="tabSelectedTextColor">?android:textColorPrimaryInverse</item>-->
    </style>

    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <!--<item name="android:textColor">?android:textColorSecondaryInverse</item>-->
        <item name="android:textColor">@color/textColorSecondaryInverseNormal</item>
        <item name="textAllCaps">false</item>
    </style>


</resources>
